{
  "name": "concord",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "pnpm -r --filter './packages/**' --filter './apps/**' build",
    "test": "pnpm -r --filter './packages/**' --filter './apps/**' test",
    "lint": "pnpm -r --filter './packages/**' --filter './apps/**' lint",
    "demo": "pnpm --filter @concord/mvp-runner dev",
    "api": "pnpm --filter @concord/coordinator-api dev",
    "concord": "pnpm --filter @concord/mvp-runner concord"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3",
    "vitest": "^3.1.2"
  }
}
